function initTyped() {
  const  options = {
    strings: ["", "<i>boo, almost forgot:</i>", "<p>Simplicity. Always.</p>"],
    typeSpeed: 80,
    startDelay: 1500
  }

  const typed = new Typed(".typed", options);
}