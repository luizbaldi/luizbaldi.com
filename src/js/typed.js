function initTyped() {
  const  options = {
    strings: ["", "<p>boo, almost forgot...</p>", "<p>simplicity, always.</p>"],
    typeSpeed: 70,
    startDelay: 1400
  }

  const typed = new Typed(".typed", options);
}